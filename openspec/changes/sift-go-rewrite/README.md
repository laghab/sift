# sift-go-rewrite

Phase 2: Rewrite Sift in Go for a single static binary with zero runtime dependencies. Inherits all features from Phase 1 (--copy, --dry-run, --search, --json, video mode, Unicode OCR) and includes the 3 unfinished tasks.
